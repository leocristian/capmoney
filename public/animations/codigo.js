

var animation = bodymovin.loadAnimation({
    container: document.getElementById('animacao'), // Required
    path: './animations/digital-finance-animation.json', // Required
    renderer: 'svg', // Required
    loop: true, // Optional
    autoplay: true, // Optional
  })